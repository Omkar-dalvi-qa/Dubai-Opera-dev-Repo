/** Home route ISR window (seconds). Used by `page.tsx` `revalidate` and homepage data fetches. */
export const LONG_REVALIDATE_SECONDS = 600; // 5 hours

export const SHORT_REVALIDATE_SECONDS = 5; // 10 minutes
