import PaymentFailed from "@/components/PaymentFailed";

export default function BookingFailedPage() {
  return (
    <div className="min-h-[calc(100vh-182px)] w-full flex items-center justify-center px-4">
      <PaymentFailed />
    </div>
  );
}